<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.parallelfalchion.gamerwatch">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name="com.parallelfalchion.gamerwatch.controllers.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.default_searchable"
                android:value="com.parallelfalchion.gamerwatch.controllers.SearchActivity" />
        </activity>
        <activity android:name="com.parallelfalchion.gamerwatch.controllers.SearchActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>

        <activity android:name="com.parallelfalchion.gamerwatch.controllers.SingleGameActivity">

        </activity>

        <activity android:name="com.parallelfalchion.gamerwatch.controllers.WishlistActivity">

        </activity>



    </application>

</manifest>